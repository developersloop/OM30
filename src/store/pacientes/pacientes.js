const state = {
    count: 0
  }

const mutations = {}
const actions = {}
const getters = {
    count: state => state.data
}


export default {
    namespaced: true,
    state,
    mutations,
    actions,
    getters,
}